import React from "react";
import Layout from "../Layout/Layout";

const Fallback = () => {
  return (
    <Layout>
      <h2 className="text-center text-peppermartDark500 text-[100px]">
        Oops something went wrong
      </h2>
    </Layout>
  );
};

export default Fallback;
